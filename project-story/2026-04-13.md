# 2026-04-13

## 11:13 — session `fc52b565`

**Problem / task:**
- what dependent vulnerabilities are there in this repo
- fix everything

**Key decisions & reasoning:**
- Here's a summary of the **25 vulnerabilities** found:  ---  ### High Severity (19)  | Package | Issue | |---|---| | `@angular/compiler` (+ core, common, forms, platform-browser, router) | XSS via i18n attribute bindings …
- The remaining ones are stuck because they require a newer version of Angular itself.

**Measured results:**
- Here's a summary of the **25 vulnerabilities** found:  ---  ### High Severity (19)  | Package | Issue | |---|---| | `@angular/compiler` (+ core, common, forms, platform-browser, router) | XSS via i18n attribute bindings …
- Good progress — down from 25 to 13.

**Complications / pivots:**
- Here's a summary of the **25 vulnerabilities** found:  ---  ### High Severity (19)  | Package | Issue | |---|---| | `@angular/compiler` (+ core, common, forms, platform-browser, router) | XSS via i18n attribute bindings …
- The most impactful ones for this project specifically are:  - **Angular XSS** — production risk if using i18n attribute bindings - **`@hono/node-server` auth bypass** — if you serve protected static files - **`hono` cook…
- The XSS fix landed in 21.2.4, and the `undici`/`picomatch` fixes come with `@angular/build` 21.2.x.
- The fix required bumping all `@angular/*` packages from `^21.1.x` to `^21.2.0` in [package.json](package.json) — the XSS, `picomatch`, `undici`, and `ajv` fixes were all gated behind Angular 21.2.x.

**Files changed:**
- `c:\Users\Ryan\Documents\GitHub\crp-tool-postmvp\package.json`

**Key commands:**
- `cd "c:\Users\Ryan\Documents\GitHub\crp-tool-postmvp" && npm audit 2>&1`
- `cd "c:\Users\Ryan\Documents\GitHub\crp-tool-postmvp" && npm audit fix 2>&1`
- `cd "c:\Users\Ryan\Documents\GitHub\crp-tool-postmvp" && npm list @angular/core @angular/compiler @angular/build @angular`
- `cd "c:\Users\Ryan\Documents\GitHub\crp-tool-postmvp" && cat package.json | grep -E '"@angular|undici|picomatch|ajv'`
- `cd "c:\Users\Ryan\Documents\GitHub\crp-tool-postmvp" && npm install 2>&1`
- `cd "c:\Users\Ryan\Documents\GitHub\crp-tool-postmvp" && npm install --legacy-peer-deps 2>&1`

---
## 15:09 — session `5594193e`

**Problem / task:**
- <ide_opened_file>The user opened the file /Users/ryangallacher/Library/CloudStorage/OneDrive-ScottLogicLtd/Documents/GitHub/crp-tool/.claude/settings.local.json in the IDE. This may or may not be related to the current t…
- merge issue?
- [Request interrupted by user for tool use]
- merge issue?
- what u recommend

**Files changed:**
- `/Users/ryangallacher/Library/CloudStorage/OneDrive-ScottLogicLtd/Documents/GitHub/crp-tool/.claude/settings.local.json`

---
