# 2026-04-10

## 09:56 — session `b88e4035`

**Problem / task:**
- <ide_opened_file>The user opened the file /Users/ryangallacher/Library/CloudStorage/OneDrive-ScottLogicLtd/Documents/GitHub/crp-tool/project-story/2026-04-01.md in the IDE. This may or may not be related to the current t…
- what would a design look like for a processing screen where we don't have the checkpoints for each step in processing, we just get started and finished back as states
- meaning the error specifically
- in terms of what components, and so we can scale the design to the stepped state
- give me a doc name

**Key decisions & reasoning:**
- The bar animates continuously rather than filling to a fixed percent, so it doesn't imply false precision.
- The indeterminate bar is more honest.
- The only real loss is if a failure happens: without a step to mark as failed, you can only say "something went wrong" rather than "it failed at extraction." If `errorCode` still comes back on failure, the existing `error…
- The step location is more useful for debugging than for user recovery.
- Yes — because the two slots are independent.
- Use a hyphen instead:  `https://example.com/no-steps-fail-extract`  That passes as a valid URL and both `no-steps` and `fail-extract` are still substrings, so both checks trigger.

**Measured results:**
- Relevant AGENTS.md sections: **Design System** (citation required before any design opinion), **Principles** (consult before recommending).
- ---  ### Finished state  The bar fills completely and holds for ~900ms (matching the current redirect delay), then the router navigates away.
- **Source:** Carbon `InlineLoading` — active → finished state transition, where the spinner resolves to a checkmark before dismissal.
- The progress bar could grey out (reduced opacity) to communicate "paused" during retry.
- That should still match — but a URL with a space is likely being rejected by form validation before the assessment is even created.
- Knowing the document was successfully retrieved removes one class of "did it hang?" anxiety before the OCR wait begins.

**Complications / pivots:**
- The bar's visual pause communicates something went wrong without needing a step to mark as failed.
- The only real loss is if a failure happens: without a step to mark as failed, you can only say "something went wrong" rather than "it failed at extraction." If `errorCode` still comes back on failure, the existing `error…
- The panel contains a heading ("There is a problem") and the actionable message.
- So the error state would look like:  ``` [heading]  Processing failed  [document chip]  📄 Supplier Contract v2.pdf  ┌─────────────────────────────────────────────────┐ │ There is a problem                              │ …

**Files changed:**
- `/Users/ryangallacher/Library/CloudStorage/OneDrive-ScottLogicLtd/Documents/GitHub/crp-tool/src/app/pages/processing/processing.html`
- `/Users/ryangallacher/Library/CloudStorage/OneDrive-ScottLogicLtd/Documents/GitHub/crp-tool/src/app/pages/processing/processing.scss`
- `/Users/ryangallacher/Library/CloudStorage/OneDrive-ScottLogicLtd/Documents/GitHub/crp-tool/src/app/pages/processing/processing.ts`

**Key commands:**
- `cd "/Users/ryangallacher/Library/CloudStorage/OneDrive-ScottLogicLtd/Documents/GitHub/crp-tool" && npm start`
- `grep -r "connectivity-banner\|banner" /Users/ryangallacher/Library/CloudStorage/OneDrive-ScottLogicLtd/Documents/GitHub/`

---
